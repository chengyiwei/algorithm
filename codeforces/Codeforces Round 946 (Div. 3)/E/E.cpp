#include <bits/stdc++.h>
using namespace std;
typedef long long ll;

void solve() {
    int m; ll x; cin >> m >> x;
    vector<vector<ll> > dp(m + 1, vector<ll>(m + 1, 0));
}

int main() {
    int t; cin >> t;
    while (t--) {
        solve();
    }
}