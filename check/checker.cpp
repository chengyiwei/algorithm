#include "testlib.h"
#include<cstring>
#include<iostream>
#include<cmath>
using namespace  std;


int main(int argc, char* argv[]) {
    registerTestlibCmd(argc, argv);
    double eps=1e-6;
    double User_out=

    quitf(_wa, "The answer is wrong: L= %d R=%d",i, j);
    quitf(_ok, "The answer is correct.");

    return 0;
}
