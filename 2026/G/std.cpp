#include <bits/stdc++.h>
using namespace std;

int main() {
    ios::sync_with_stdio(false);
    cin.tie(0); cout.tie(0);

    int N, M; cin >> N >> M;
    if (N % 2 == 0 && M % 2 == 0) cout << "No\n";
    else cout << "Yes\n";
}