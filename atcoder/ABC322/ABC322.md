# [AtCoder Beginner Contest 322](https://atcoder.jp/contests/abc322)

## A - First ABC 2

时间：2s，空间：1024MB

### Problem Statement

有一个长度 $N$ 的字符串 $S$ 包含 A，B，C

寻找第一个ABC出现的位置

### solution

SB题

### code

```cpp
#include<bits/stdc++.h>
using namespace std;
int main(){
    int N;
    cin >> N;
    string s;
    cin >> s;
    for(int i = 0; i < N-2; i++){
        if(s[i] == 'A' && s[i+1] == 'B' && s[i+2] == 'C'){
            printf("%d\n",i+1);
            return 0;
        }
    }
    printf("-1");
    return 0;
}
```



## [B - Prefix and Suffix](https://atcoder.jp/contests/abc322/tasks/abc322_b)

### Problem Statement

有两个由小写字母组成的字符串 S 和 T ，S 的长度为 N ，T 的长度为 M，其中 $N \le M$ 

- 如果 S 是 T 的前缀并且是后缀，输出 0
- 如果 S 是T 的前缀并且不是后缀，输出 1
- 如果 S 是 T 的后缀并且不是前缀，输出 2
- 如果 S 既不是 T 的前缀也不是后缀，输出 3

### solution

SB 题

### code

## [C - Festival](https://atcoder.jp/contests/abc322/tasks/abc322_c)

### Problem Statement

AtCoder王国举办了一个 $N$ 天的节日，这其中的 $M$ 天 $A_1,A_2,A_3,...,A_M$ ，这些天里面会有烟火表演，确保在节日的最后一天会有验货表演，也就是是说 $A_M = N$ 

对于每一个 $i=1,2,...,N$ 解决下列问题

第 $i$ 后最近的一场烟火，距离第 $i$ 天还有多久

### solution

SB题

### code

```cpp
#include<bits/stdc++.h>
using namespace std;
int N, M, day[200005];
const int INF = 0x3f3f3f3f;
int main(){
    cin >> N >> M;
    memset(day, INF, sizeof day);
    for (int i = 1; i <= M; i++)
    {
        int fire;
        cin >> fire;
        day[fire] = 0;
    }
    for (int i = N - 1; i; i--)
        day[i] = min(day[i], day[i+1] + 1);
    for (int i = 1; i <= N; i++)
        cout << day[i] << endl;
    return 0;
}
```



## D - Polyomino

### Problem Statement

polyomino是一种由几个正方形的边缘连接而成的连接多边形形状的拼图。

![](D:\workspace\C++\routine_training\atcoder\ABC322\ABC322.assets\81e983f85e958e0d612063adcc455c71.jpg)



有一个四行四列的网格，网格中有三个polyominoes 

给出三个polyominoes ，通过使用` # `和 `.` 

我们想知道是否可以使用着三个来铺满整个网格，需要满足以下条件：

- polyominoes 不能重叠
- polyominoes 不能伸出网格
- polyominoes 可以旋转但是不能翻转

