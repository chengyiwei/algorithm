#include <bits/stdc++.h>
using namespace std;
int main() {
    freopen ("G.in", "w", stdout);
    int n, q; cin >> n >> q;
    cout << n << ' ' << q << '\n';
    vector<int> a(n + 1);
    for (int i = 1; i <= n; i++)
        cin >> a[i];
    for (int i = 1; i <= q; i++) {
    
    }
}