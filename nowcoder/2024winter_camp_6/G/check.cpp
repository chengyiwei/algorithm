#include <bits/stdc++.h>
using namespace std;
int main() {
    int T; scanf("%d", &T);
    while (T -- ) {
        int n , S, k; scanf("%d%d%d", &n, &S, &k);
        vector<int> a(n + 1);
        for (int i = 1; i <= n;)
    }
}